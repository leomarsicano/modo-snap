- Ajudei o Leo a configurar SSH com GitHub numa VPS para subir o repositório `modo-snap`. O caminho que funcionou foi recriar a chave `~/.ssh/id_ed25519`, adicionar a nova chave pública no GitHub, configurar `~/.ssh/config` para `github.com`, validar com `ssh -T git@github.com`, identificar que a branch local era `master` e que o erro final era porque o repositório ainda não existia. Depois de criar o repo no GitHub, o push `git push -u origin master` funcionou.
- Também ajudei o Leo a conectar o repo `modo-snap` na Vercel. Descobrimos que o app real está dentro da pasta `pessoal-dashboard` e é um projeto `Vite + React + TypeScript`. A configuração correta na Vercel ficou com `Framework Preset: Vite`, `Root Directory: pessoal-dashboard`, `Build Command: npm run build` e `Output Directory: dist`. Também orientamos a desligar `Vercel Authentication` para facilitar o teste e a fazer `Redeploy` do deploy mais recente.
