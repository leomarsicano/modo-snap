- Hoje avancei com o Leo no projeto `pessoal-dashboard` em `/root/.openclaw/workspace/pessoal-dashboard`.
- Separação do check-in em duas seções no app: `Rotinas` e `Alimentação`.
- O Leo tem dificuldade com edição via terminal/nano, então funciona melhor guiando passo a passo com prints e instruções curtas.
- O app foi testado pela VPS da Hostinger, rodando com `npm run dev -- --host 0.0.0.0 --port 4173`.
- A porta 5173 estava bloqueada externamente, mas a 4173 abriu.
- Problema diagnosticado no Supabase: a tabela `routine_items` estava vazia; o Leo populou os itens via SQL Editor.
- Problema adicional: o update de status no app falhava por mismatch de ids e depois por lógica frágil de recarga.
- Ajustes feitos no app ao longo da sessão incluíram fallback de update por `title`, recarga completa da rotina após salvar, e remoção da dependência da coluna `category` nas queries ao banco.
- O Leo quer o app simples e funcional, sem complexidade técnica desnecessária.
