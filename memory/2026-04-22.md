- Leo relatou que o resumo de e-mails das 07:30 não chegou na prática, apesar do cron ter status ok; ajustado para 07:30 e texto mais evidente.
- Projeto correto para a agenda da AutoHolic é `autoholic-agenda`, não `pessoal-dashboard`.
- Foi criada automação no `autoholic-agenda` para envio automático via Z-API de reconfirmação 5 dias antes e lembrete 3 dias antes, ambos às 09:00 (Brasília), reaproveitando as mensagens dos botões já validadas manualmente.
- Arquivos adicionados/ajustados no `autoholic-agenda`: `api/send-scheduled-whatsapp.ts`, `vercel.json`, `supabase.sql`, `README.md`.
- No Supabase, Leo já executou o SQL para adicionar `reconfirmation_sent_at` e `reminder_sent_at` na tabela `appointments`.
- Na Vercel, já existiam `ZAPI_CLIENT_TOKEN`, `ZAPI_INSTANCE_ID`, `ZAPI_INSTANCE_TOKEN`, `VITE_SUPABASE_URL` e `VITE_SUPABASE_ANON_KEY`; foi adicionada `SUPABASE_SERVICE_ROLE_KEY`.
- Houve falha inicial de deploy por erro de TypeScript no endpoint novo; correção aplicada e push feito no branch `master`.
- Status no momento do flush: deploy da Vercel ficou `Ready`; próximo passo imediato é Leo testar o endpoint `/api/send-scheduled-whatsapp` no domínio atual do projeto.
- Ficou combinado que quando Leo pedir para ler o e-mail particular, isso significa ler o Gmail pessoal.
- Ficou combinado que quando Leo pedir para ler o e-mail da empresa, isso significa ler `leonardo@autoholic.com.br`.
- Ficou combinado que quando Leo pedir para “ler todos os e-mails”, isso significa ler os dois: particular e empresa.
